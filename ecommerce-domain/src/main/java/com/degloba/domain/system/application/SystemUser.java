package com.degloba.domain.system.application;


public class SystemUser {

	/*
	 * public Key getDomainUserId(){ return KeyFactory.stringToKey(
	 * UUID.randomUUID().toString()); //return new Key("1");//TODO introduce
	 * security integration }
	 */
}
